# using the split and join methods
# convert a string to list, remember to pass list(hello) e.g. ['h', 'e', 'l', 'l', 'o']
#print(list('hello'))

# split method , this will append elements to a list
colors = 'red,green,blue'
new_colors = colors.split(',')
print(new_colors)
