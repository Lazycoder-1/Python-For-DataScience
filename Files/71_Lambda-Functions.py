
# Lambda Functions = functions defined without using the def keyword , start with lambda instead
# They are called anonymous functions because that don’t have a name like a normal function

# with only one parameter
# double = lambda num: num * 2
# print(double(5))

# with 2 or more parameters
multi = lambda a,b,c: (c-a)/b
print(multi(10,4,8))


